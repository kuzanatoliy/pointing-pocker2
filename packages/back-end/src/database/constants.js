const TABLES = Object.freeze({
  USERS: "Users",
});

const FIELD_LENGTHS = Object.freeze({
  AUTH_ID: 50,
  EMAIL: 60,
  FIRST_NAME: 50,
  LAST_NAME: 50,
  PROVIDER: 20,
});

module.exports = {
  TABLES,
  FIELD_LENGTHS,
};
